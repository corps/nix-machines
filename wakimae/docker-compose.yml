services:
  app:
    build:
      context: .
    ports:
      - 8081:8080
    volumes:
      - ./src:/app/src
      - db:/var/sqlite/wakimae
      - store:/var/store/
      - ./.env:/run/env

volumes:
  db:
  store:
